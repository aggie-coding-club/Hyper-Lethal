public enum AIStates
{
    always,
    wandering,
    attacking,
    idling
};

public enum ControlledBy
{
    autonomous,
    aiControlled,
    playerControlled,
}

public enum EntityType
{
    other,
    ship,
    station,
    turret,
    projectile,
}

